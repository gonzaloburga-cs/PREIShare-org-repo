/**
 * App config for the PREIshare TanStack Start scaffold.
 *
 * This version of @tanstack/react-start is configured with the Vite plugin in
 * vite.config.ts. `npm run dev` reads that file. This module re-exports it so
 * the scaffold has an app.config.ts entry without a second, conflicting setup.
 *
 * Dashboard child routes (portfolio, deals, profile) are not defined here.
 */
export { default } from './vite.config.ts'
